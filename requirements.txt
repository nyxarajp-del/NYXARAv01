# NYXARA — full stack. Core deps are required; heavy ML deps degrade gracefully.
# Core
pydantic>=2.6
pydantic-settings>=2.1
# Reasoning / math / data
z3-solver>=4.12
sympy>=1.12
numpy>=1.26
networkx>=3.2
scikit-learn>=1.4
# LLM providers
anthropic>=0.40
openai>=1.40
httpx>=0.27
# Groq cloud (e.g. openai/gpt-oss-120b) is OpenAI-wire-compatible — served via the openai SDK above.
# Qwen3 local open-source model (optional, heavy — import-guarded in code; see pyproject [qwen] extra):
# transformers>=4.51
# torch>=2.2
# accelerate>=0.30
# Vector / retrieval
faiss-cpu>=1.8
# Security
cryptography>=42.0
# Observability
structlog>=24.1
# Dev / test
pytest>=8.0
pytest-asyncio>=0.23
# Senses (heavy — optional; import-guarded in code)
# torch>=2.2
# openai-whisper>=20231117
# soundfile>=0.12
# pillow>=10.0
# opencv-python>=4.9
# pdfplumber>=0.11
# python-docx>=1.1
# openpyxl>=3.1
